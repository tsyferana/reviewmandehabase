import 'package:flutter/material.dart';

class AppColors {
  // Brand colors (also used in themes)
  static const Color primary = Color(0xFF4D96FF); // Modern blue
  static const Color secondary = Color(0xFFFF9500); // Warm orange
  static const Color tertiary = Color(0xFF17B890); // Teal

  // Semantic & status colors
  static const Color success = Colors.green;
  static const Color error = Colors.red;
  static const Color warning = Colors.orange;
  static const Color starRating = Colors.amber;

  // Neutral colors
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color transparent = Colors.transparent;
  static const MaterialColor grey = Colors.grey;
}
